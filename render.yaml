services:
  - type: web
    name: spacy-fr-lemmatiseur
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python -m spacy download fr_core_news_sm
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
